import { RuleTriageWizard } from '../../components/healthcare/RuleTriageWizard';

export function SymptomChecker() {
  return (
    <RuleTriageWizard
      title="Symptom Checker"
      subtitle="A rule-based care guidance tool. Answer honestly — it only takes a minute."
    />
  );
}